
export class ActionManager {
  // Here we will couple action and reducer because it is much more convenient to have them in one place.
  // Here some action creator wrappers will be implemented to fasten action implementaiton processs.
  // Convenient accessor methods to get specific states like : getDictionaryFor(state,'products') or getProducts(state), getPricelists(state);
}
